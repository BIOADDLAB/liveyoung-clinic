import ResetPasswordContent from "./ResetPasswordContent";

export const metadata = {
    title: "비밀번호 재설정 | 리브영클리닉",
    description: "새로운 비밀번호로 재설정합니다.",
};

export default function ResetPasswordPage() {
    return <ResetPasswordContent />;
}
